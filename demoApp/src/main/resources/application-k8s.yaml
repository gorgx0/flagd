spring:
  application:
    name: flagd-demo-app
gg:
  jte:
    use-precompiled-templates: true
#    development-mode: true

openfeature:
  flagd:
    deadline: 3000
    config-type: IN_PROCESS # IN_PROCESS/RPC
    rpc:
      host: flagd-resolve
      port: 8013
    in-process:
      host: flagd-sync
      port: 8015
