spring:
  application:
    name: flagd-demo-app
gg:
  jte:
    use-precompiled-templates: true
#    development-mode: true

openfeature:
  flagd:
    config-type: IN_PROCESS # IN_PROCESS/RPC
    host: flagd
    rpc:
      port: 8013
    in-process:
      port: 8015
