spring:
  application:
    name: flagd-demo-app
gg:
  jte:
    use-precompiled-templates: true
#    development-mode: true

openfeature:
  flagd:
    deadline: 3000
    config-type: IN_PROCESS # IN_PROCESS/RPC
    rpc:
      host: resolve.flagd.minikube
      port: 30080
    in-process:
      host: sync.flagd.minikube
      port: 30080
