spring:
  application:
    name: flagd-demo-app
gg:
  jte:
    use-precompiled-templates: true
#    development-mode: true

openfeature:
  flagd:
    config-type: IN_PROCESS # IN_PROCESS/RPC
    rpc:
      host: ofrep.flagd.minikube
      port: 30080
    in-process:
      host: grpc.flagd.minikube
      port: 30080
